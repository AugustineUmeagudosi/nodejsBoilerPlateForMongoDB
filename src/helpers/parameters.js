//users
const topicDetails = [
    'id', 'topic', 'createdAt'
],

subscriberDetails = [
    'id', 'url', 'topicId'
];

messageDetails = [
    'id', 'topicId', 'data'
];

module.exports = {
    topicDetails, subscriberDetails, messageDetails
};